const value = 3;
console.log(value + 4 + value);

/* Value is the string "3". When concatenating a sting with
    a number, the number will be coerced into a string. 
    This results in three strings being put together to form a
    three character string. */
